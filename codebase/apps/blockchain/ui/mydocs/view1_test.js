'use strict';

describe('myApp.myDocsView module', function() {

  beforeEach(module('myApp.myDocsView'));

  describe('myDocsView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var myDocsViewCtrl = $controller('myDocsViewCtrl');
      expect(myDocsViewCtrl).toBeDefined();
    }));

  });
});
