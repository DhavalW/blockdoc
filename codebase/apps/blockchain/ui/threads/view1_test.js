'use strict';

describe('myApp.threadsView module', function() {

  beforeEach(module('myApp.threadsView'));

  describe('threadsView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var threadsViewCtrl = $controller('threadsViewCtrl');
      expect(threadsViewCtrl).toBeDefined();
    }));

  });
});
